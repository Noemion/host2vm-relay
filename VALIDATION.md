# 验证记录

## v0.2.0 安装包与运行依赖

- 三种架构均按固定发布配置打包，应用目录只有单个 EXE，无需补放 DLL 或运行时配置文件。
- Windows 11 x64 上，x64 和 x86 发行包在 DOTNET_ROOT 指向不存在目录、DOTNET_MULTILEVEL_LOOKUP=0 的情况下通过全部自检。
- .NET 启动跟踪确认 `Detected Single-File app bundle` 和 `Executing as a self-contained app`，内置运行时版本为 8.0.31。
- PE 架构检查：x64 为 0x8664、x86 为 0x014C、ARM64 为 0xAA64。
- 离线安装器实际安装成功；选中的 EXE 与 x64 发布文件 SHA-256 一致。
- 安装后的程序四个页面启动和渲染检查通过；手动重连控件回归检查通过。
- 卸载成功，应用 EXE 和测试安装记录移除。安装和卸载不删除用户设置目录。
- ARM64 没有目标设备实测；Windows 10 未在独立系统中实测。上述结果不等于全部 Windows 环境兼容性认证。
- 当前安装包未签名；GitHub Actions 工作流的远程执行情况以仓库页面为准。

构建环境：Windows x64，.NET SDK 8.0.425；SSH.NET 2026.0.0。Release 构建零警告、零错误。

已验证：

- 规则标准化、去重、精确域名、域名后缀、IPv4/IPv6、CIDR，以及拒绝无效输入。
- Windows DPAPI 密码加解密；配置文件只包含密文。
- 本机 HTTP 规则服务、动态更新和未知路径的 404 响应。
- 扩展脚本保留 mieru UDP 逻辑、保留其他规则、重复应用不会增加重复节点，以及原有 Fake-IP 黑名单/白名单/规则模式的转换。
- 电脑现有 Mihomo v1.19.31 对生成配置的语法校验成功。
- 独立 Mihomo 测试实例对 code.example.com 返回 198.18.0.4 Fake-IP，未改动正在使用的 Clash 配置。
- 本地模拟 SSH 服务器的密码登录与 Ed25519 私钥登录，通过 SOCKS5 将一个仅由远端处理的测试域名转发并返回 HTTP 响应；主机指纹核对。
- 自包含发行 EXE 启动、四个页面渲染检查和发行包自检。
- 回归检查：虚拟机 IPv4/IPv6 绕过规则根据配置生成；断线后手动连接控件恢复；密码模式不开放私钥浏览。
- GitHub Actions 构建工作流已添加；尚未在 GitHub 上触发远程运行。

实际麒麟账号认证、aTrust 和 Chrome/TUN 完整链路需要在用户输入凭据并导入脚本后验证。本次没有读取用户密码或私钥，没有修改运行中的 Clash 配置。

免密推荐使用 Ed25519。测试中的旧式 RSA 签名被模拟服务器拒绝，未验证 RSA 在用户麒麟服务器上的兼容性。应用不会降低服务器的签名安全设置。
